//==================================
// Foundations of Computer Science
// Student: Xinyan Wu
// id: a1809534
// Semester: S1
// Year:2021
// Practical Exam Number: 02
//===================================

public class HandlingArrays {

    public static void printArray(double [] testArray) {

          System.out.print("printing Array: [");
          for(int i = 0; i < testArray.length; i++) {
          	if(i == testArray.length - 1) {
          		System.out.print(testArray[i]);
          	} else {
          		System.out.print(testArray[i] + ",");
          	}
          }
     }

    public static double[] sumElements(double [] firstArray, double [] secondArray) {
    	// Define a new array
        double[] thirdArray = new double[firstArray.length];

        // Determine whether the length of firstArray  and secondArray are equal.
		if( firstArray.length != secondArray.length ){
			throw new RuntimeException("Error - Arrays different shape");
        // Perform this step if the length is equal
        } else {
			for (int i = 0; i < firstArray.length; i++) {
				thirdArray[i] = firstArray[i] + secondArray[i];
            }
        }
		return thirdArray;
     }


    public static double[] reverseArray(double [] testArray) {
    	// double temp;
    	// /*
    	// Loop through the array and define two index variables. 
    	// The judgment condition is that the start index is less than or equal to the end index 
    	// */
    	// for (int start = 0, end = testArray.length - 1, start <= end, start++, end--) {
    	// 	//Variable exchange
    	// 	double temp = testArray[start];
    	// 	testArray[start] = testArray[end];
    	// 	testArray[end] = temp;
    	// }

          
     }



}